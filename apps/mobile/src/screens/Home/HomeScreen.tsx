import { View, Text } from 'react-native';

// Category shortcuts (hotels, salons, rentals, flights, trains) + featured listings
export function HomeScreen() {
  return (
    <View>
      <Text>Zhaan</Text>
    </View>
  );
}
